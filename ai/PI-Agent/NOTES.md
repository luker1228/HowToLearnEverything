# Notes

- 用户偏好中文交流和中文课程内容。
- 用户当前工作目录 `ai/PI-Agent/` 同时是代码目录（`pi/`）和教学工作区根目录——两者共用一个目录，注意不要把教学文件和实际代码混淆。
- `ai-agent-book/` 是仓库里已经存在的独立资源（李博杰的开源书），非常契合本次 mission，已收录进 `RESOURCES.md`，应作为通用理论的主要引用来源。
- 首次会话通过 AskUserQuestion 确认：用户目的是「系统性理解 Agent 架构」（把 pi-agent-core 当参照实现），背景是「TS 熟练，Agent/LLM 新手」。已写入 `learning-records/0001-prior-background.md`。
- 用户会在课后主动提出假设让我验证/纠正（而不是被动听讲），并且期望聊天里给出的纠正/澄清**立刻**同步进对应的 reference 卡片，不要只停留在聊天记录里——聊天是一次性的，reference 卡片才是他会回来查的地方。以后每次在聊天里做了实质性纠正，随手检查一下是否要点进对应 reference 文件。
- 用户明确要求教学要「从问题到答案」的角度组织——先讲清楚在解决什么实际问题，再讲代码细节，而不是反过来；他自己不一定能提前想到该问什么问题，所以老师要主动预判、提出他没想到的问题。完整版本已写进 `CLAUDE.md`（这个文件 Claude Code 每次进这个目录都会自动读取，NOTES.md 只放摘要）。
- Lesson 6 初版踩了这个坑：把"为什么拆两层"的宏观动机放到了全课最后一节，前面几节直接从函数签名/代码细节开始讲，用户反馈"感觉没有从宏观上讲，没看懂在讲什么"（已重写，问题先行）。教训：写一节课之前，先检查开头 1-2 段是否已经用非代码的语言把"这段机制在解决什么具体麻烦"讲清楚——不能只满足于整堂课的开场白提了类比，每一小节引入新代码前也要有一句话说清楚"这段代码对应的是前面提出的哪个诉求"。参照 Lesson 5 第 1 节（先给出具体数据，36.7 万字符/128K token 溢出的例子，再进代码）作为"问题先行"的标杆样例。
- 重要纠正（2026-08-24，Lesson 6 重写后立刻出现）：用户明确要求**不要用比喻/类比**讲解——包括"仪表盘/引擎/油箱"这种现实世界类比，也包括拿其他框架（如 Redux 的 reducer/subscribe）做对比。要直接用代码里的类名、函数名、专有名词说话（例如：`Agent` 类实例 / `runAgentLoop` 函数 / `processEvents` 方法，而不是"仪表盘"/"引擎"/"reducer"）。**注意"从问题到答案"≠"用比喻"**——问题先行讲的是先说清楚"这段代码在解决什么具体麻烦"，答案部分仍然要落到真实的代码实体和专有名词上，不能靠类比现实物体或其他框架来替代对代码本身的解释。已把 Lesson 1、Lesson 6（及各自 reference 卡片）里的"仪表盘/引擎/Redux"说法全部替换成 `Agent` 类 / `runAgentLoop` 这类直接代码术语。
- 2026-08-24：用户指定 [Pi Agent Book（dg-ai-notes）](https://dg-ai-notes.pages.dev/) 作为本 workspace 结构的主要参照——这是专门讲 `pi-agent-core` 本身的中文教程站（理论 track M01-M10 + 实践 track P01-P07），已收录进 `RESOURCES.md` 并附课程覆盖映射表。后续新课优先对齐它的章节顺序，缺口候选：M04 模型调用、M07 事件驱动完整版、M10 会话管理、整个实践 track（部署相关，按 MISSION 目前范围暂不强制要求动手）。
- 重要 gotcha（Lesson 9 发现，2026-08-24）：`pi/packages/agent` 里不是所有"看起来像整合逻辑"的代码都是真实现——`harness/agent-harness.ts` 的 `AgentHarness` 类几乎每个方法（包括 `skill()`）都只是 `this.unavailable(...)` 空壳，抛 `HarnessNotImplemented`。遇到"这个特性到底怎么被使用/被决策"这类问题，如果在 `packages/agent` 里只找到类型定义和格式化函数、找不到真正调用它们做决策的地方，下一步应该去 `pi/packages/coding-agent`（pi 这个 CLI 产品本身的源码）找——那里才是真正把这些积木组装起来跑的地方。目前 mission 主参照仍是 `packages/agent`，但遇到这种"空实现"要主动扩大搜索范围到 `coding-agent`，并在课程里明确标注引用范围变了（不要默默切换却不说）。
- 教训（Lesson 9 第一版出错，2026-08-24）：写"两条路径都存在，哪条是主路径"这类判断时，光靠读代码反推容易得出错误的主次结论——第一版 Lesson 9 因为只看了源码，就断言"用户敲 /skill:name 是主路径，模型自主发现是次要路径"，被用户当场质疑（"应该是 agent 发现我的技能，不可能每个技能都靠我主动 call"）。后来在 `coding-agent/docs/skills.md`「How Skills Work」找到官方文档明确写着相反的主次关系（模型自主用 read 加载是默认设计，`/skill:name` 是"模型不总是会自己发现"之后加的兜底）。**教训：涉及"哪个是默认/主要路径"这类设计意图判断时，优先找官方文档（README、docs/*.md）里的明确说法，不要只靠代码里两条路径都存在就自己猜权重/顺序**——代码能证明"两条路径都实现了"，证明不了"哪条更常用/更被推荐"。已修正 Lesson 9 和对应 reference 卡片的框架。
- 反复出现的模式（2026-08-24 起，已在 Lesson 9 `AgentHarness.skill()`、learning-record 0006 的 `prepareArguments`、Lesson 11 的 `AgentTool.executionMode: "sequential"` 三处独立出现）：**pi 里"类型定义齐全、字段/钩子确实存在"≠"真的被用上"**。遇到"这个机制目前实际发挥作用吗"这类问题，光找到字段/接口定义不够，还要 grep 一遍看有没有任何调用点/任何内置实现真的赋了这个值——没有，就要在课程里明确标注"通用回调点已经造好，但目前没人用"，不要因为类型系统里存在就默认它在生效。
- 2026-08-24，新增 Lesson 11（`lessons/0011-parallel-tool-execution.html` + `reference/0011-parallel-tool-execution.html`）：讲一批 toolCall 之间"parallel"模式下 prepare 顺序、execute 并发、事件顺序与消息顺序分离保证——由用户主动提出"pi 如何并行执行工具，值得研究"驱动，属于 learning-record 0002 当时留下的一个未展开的口子，这次补上了。
- 用户明确要求记住的一条事实：**LLM 认识的消息只有三种**——`@earendil-works/pi-ai` 的 `Message` 类型只有 `user`/`assistant`/`toolResult` 三种角色，这是 pi 里唯一真正会被发进 LLM 请求的格式。已经在 `reference/0004-context-pipeline.html:37` 那张表里写清楚了（`AgentMessage` 才是内部丰富格式，`convertToLlm` 负责把它收窄成这三种之一，收不回去的走 `excludeFromContext`）。之所以特别记一笔：这条边界是理解 Lesson 4/12（`AgentMessage` vs `Message`、pi 没有 RAG/长期记忆层）的地基，后续任何"Agent 内部消息类型 vs LLM 看到的消息类型"的讨论都要先确认对方没有混淆这条边界。
- 2026-08-24，新增 Lesson 13（`lessons/0013-thinking-content.html` + `reference/0013-thinking-content.html`）：讲 `ThinkingContent`（跟 `TextContent`/`ToolCall` 平级的第三种内容块）和 `thinking_*` 三元组事件（跟 `text_*`/`toolcall_*` 结构对称，`agent-loop.ts` 同一段代码处理），以及 `Agent.thinkingLevel → config.reasoning → streamFunction` 的开关链路。由用户主动问"模型回复带思考过程是怎么实现的"驱动。留了一个没验证的追问点：压缩（Lesson 5）遇到带 `thinkingSignature` 的消息具体怎么处理，还没看代码确认。
- 待续任务（未完成，2026-08-24）：Lesson 13 之前在筹备一个 RAG 动手实验小节（用户选了"概念课+动手跑实验"），计划用 `ai-agent-book/chapter3/dense-embedding/cli.py` 做 Lesson 13（当时的编号，现在应该是 Lesson 14 起）第一课，跑 `--eval` 或离线 `--compare-ann`。卡在环境准备：容器里没装 `sentence-transformers`，`pip`/`python3 -m pip` 被沙箱分类器拦了（"transient" 报错，重试可能会过，也可能需要 `dangerouslyDisableSandbox` 或用户手动装依赖）。之后被"思考过程怎么实现"这个新问题打断，还没接回去——下次开新会话如果用户提到"继续 RAG"或"上次卡住的环境问题"，先看这一条，不要重新从头设计课程结构（`--compare-ann` 是完全离线、不需要下载模型的合成向量对比模式，可以作为不依赖网络/大模型下载的第一步备选）。
