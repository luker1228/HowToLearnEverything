# 别把 AI 当微信聊：一篇讲清 Token 膨胀和上下文管理的文章

![别把 AI 当微信聊：一轮对话只做一件复杂事](https://luke-1307356219.cos.ap-chongqing.myqcloud.com/articles/01-infographic-one-task-cover.png)

不知道为什么，在做一些长对话的时候，发现 token 消耗的速度特别快，尤其是我引入的上下文很多的时候，哪怕后期在干一些简单的事情，token 都是几块几块钱的费，（当时主要在改前端的 UI，对话里复制了很多 HTML 源码），但当时手里其实没什么硬证据。后来越用越觉得这个直觉不像错觉，就专门深入研究了一下这个问题。

研究到最后，我发现背后其实是一个很朴素、但大部分人没意识到的机制：**每一轮对话，AI 都要把这轮之前的全部聊天记录重新读一遍**。

也正因为如此，一个 AI 助手对话刚开始时往往又快又准，聊到后面却会明显变慢、变"迟钝"，偶尔还会莫名其妙忘掉你前面强调过的细节，账单涨得也比预期快。

---

## 一、agent loop 的伪代码

AI 模型本身是"无状态"的——它不会自己记住上一轮说了什么。每次你发一条消息，助手实际上是把"从对话开始到现在的所有内容"整包重新发给模型，让它从头读一遍再回答。

请记住这句："从对话开始到现在的所有内容"。如果没有理解，或者你不知道 agent loop 原理，没关系，我来带你理解。

```
messages = [ { role: "system", content: SYSTEM_PROMPT } ]

function on_user_turn(user_text):
    messages.append({ role: "user", content: user_text })

    while True:
        # 关键点：每次调用，都是把 messages 这个不断变长的数组整个发出去
        response = call_llm(messages)
        messages.append({ role: "assistant", content: response })

        if response.tool_call is not None:
            result = execute_tool(response.tool_call)
            messages.append({ role: "tool_result", content: result })
            continue   # 还在同一个"用户轮次"里，回头再调一次 LLM
        else:
            break      # 这轮结束，把 response 显示给用户，等下一条输入
```

要点其实是两行加在一起，缺一不可：

```
messages.append(...)             # 内容只增不减，数组越转越长
response = call_llm(messages)    # 但每次调用，发的是整个数组，不是新增的那一点
```

这两行代码会让 message 的长度增长速度变为平方级别。

数组在不断变长（`append`），而每一次调用又要把这个已经变长的数组**整个**重新处理一遍（`call_llm(messages)`）。第 k 次调用要处理的量 ≈ 当时数组的长度 ≈ 前面已经 append 过的总量；把第 1 到第 N 次调用的处理量加起来，就是文章开头那个"平方级增长"的来源。

而且真正在膨胀的粒度，是"这个 `while` 循环转了多少圈"——只要模型这一步决定调用工具，内层 `while` 就会再转一圈，再调一次 `call_llm(messages)`，而这时 `messages` 已经比上一次调用又长了一截。

---

## 二、具体对话演示

假设用户依次说了三句话，每句都会触发一次工具调用。用 token 数打个大概比例（数字是示意，不是精确值）：

把整个过程拆成一步步的"账本"，每一行都是"上一行累计 + 这一步新增"，标 🔴 的行就是真正触发 `call_llm()` 的时刻——那一行的"累计"就是这次调用实际要发送、要处理的量：

| 步骤 | 发生的事 | 这一步新增 | messages 累计大小 |
|---|---|---|---|
| 0 | 系统提示词加载 | +2000 | 2000 |
| 1 | 用户："帮我看下 utils.py 里的 add 函数是否有 bug" | +500 | 2500 |
| 2 | 🔴 调用① `call_llm(messages)` —— 发送 2500 | — | 2500 |
| 3 | 模型决定调用 `read_file("utils.py")` | +300 | 2800 |
| 4 | 工具返回：utils.py 文件内容 | +3000 | 5800 |
| 5 | 🔴 调用② `call_llm(messages)` —— 发送 5800 | — | 5800 |
| 6 | 模型给出最终回答："add 函数没问题" | +1000 | 6800 |
| 7 | 用户："顺便把它测试一下" | +200 | 7000 |
| 8 | 🔴 调用③ `call_llm(messages)` —— 发送 7000 | — | 7000 |
| 9 | 模型决定调用 `run_bash("pytest")` | +300 | 7300 |
| 10 | 工具返回：测试输出 | +2000 | 9300 |
| 11 | 🔴 调用④ `call_llm(messages)` —— 发送 9300 | — | 9300 |
| 12 | 模型给出最终回答："测试通过" | +800 | 10100 |
| 13 | 用户："再检查一下 subtract 函数" | +300 | 10400 |
| 14 | 🔴 调用⑤ `call_llm(messages)` —— 发送 10400 | — | 10400 |
| 15 | 模型决定再次调用 `read_file("utils.py")`（同一个文件也要重新读） | +300 | 10700 |
| 16 | 工具返回：文件内容又发一次 | +3000 | 13700 |
| 17 | 🔴 调用⑥ `call_llm(messages)` —— 发送 13700 | — | 13700 |
| 18 | 模型给出最终回答："subtract 函数有个边界条件漏了" | +1200 | 14900 |

对着这张表能直接看出两件事：

1. **"累计大小"这一列只会往上涨，从没降过**——这就是 `messages.append` 只增不减的效果，数组从 2000 一路涨到 14900。
2. **每个 🔴 行的数字，就是当次 `call_llm` 真正要处理的量**——不是"这一轮新增了多少"，而是"到目前为止攒了多少"。

把 6 次调用（🔴 行）实际处理的字数加起来：

```
2500 + 5800 + 7000 + 9300 + 10400 + 13700 = 48700
```

用户只说了 3 句话、不到 400 字的"净内容"，但因为每一次 `call_llm` 都要重发到当时为止的**全部**历史，6 次调用累计实际处理的字数堆到了 48700——而且能明显看到，**第 15 步甚至重复读了第 4 步已经读过的同一个文件**。历史越长，这种"重复读旧东西"的比例只会越高，累计处理量随轮数近似**平方级**增长。

如果后面你本来一句话就能说清楚的事情，比如：1. 先帮我创建新文件；2. 帮我写一篇博客；3. 补充我的微信二维码。

你分成 3 轮输出。

假设 LLM 的返回 message 很小，接近于零（这里纯粹是为了方便计算）。你这三轮对话，会让 token 上下文即 messages 积累到 48700 * 3。

核心原因是因为你看起来是和 LLM 一句话一句话对话。这里的 Agent 本质却是记录你的每轮对话，要每次都把所有历史发给 LLM，才让它有了"记忆"。

---

## 三、小测验：给源代码 vs 给引用，哪个更费 token？

**先看题**

假设你要 AI 帮你检查一个 300 行的函数有没有 bug，有两种问法：

- **问法 A**：把这 300 行代码整段直接贴进你的消息里，问"这段代码有没有 bug"
- **问法 B**：只说"帮我看下 `utils.py` 这个文件"，让 AI 自己调用 `read_file` 工具去读这份代码

这一句话本身，两种问法处理的 token 量差不多（A 是直接贴进去，B 是通过工具读进来，代码内容量是一样的）。**但如果接下来你还要在这个对话里接着问 5 件不相关的事，哪种问法会让后面的对话更快堆起来、更难清理？**

（先自己想一下，再往下看分析）

---

**分析**

答案是：**问法 A 更费，而且是"暗亏"——账单上第一时间根本看不出来，要等对话变长以后才会显现出差距。**

区别不在"内容量"，而在"这份内容被存在了 `messages` 数组的什么位置"：

- **问法 A（直接贴源码）**：这 300 行代码是这条 `user_message` 的正文，跟你的问题文字焊在一起、是同一条消息。以后不管你再问多少件跟它无关的新问题，这 300 行都要跟着这条 `user_message` 一起被反复重发，而且没法被单独摘掉——唯一能清理它的手段是"整体摘要压缩"，要等历史逼近上限才会触发，而且一压缩就是有损的。
- **问法 B（给引用，让 AI 自己读）**：同样这 300 行代码，存的是这次 `read_file` 调用产生的 `tool_result`，跟你的问题文字是两条独立的消息。`tool_result` 是"工具产物"，不是"你说的话"——可以被单独识别、单独清理：这份内容用完之后，后面不再需要时，可以直接把这条 `tool_result` 从热上下文里删掉或换成占位符，不用动用一次昂贵的摘要调用；真要用的时候，让 AI 重新读一次就行。

一句话总结：**贴源码 = 焊死在对话里，只能等最重的压缩来救；给引用 = 存在一个"随时可丢、按需再取"的位置，清理成本低得多。** 第一次送进去的成本几乎一样，但后面几十轮要不要一直背着它，天差地别。

---

## 四、真正的解法是压缩，但压缩不是免费的

有些时候，如果你发现你的上下文膨胀过快，可能就是因为你的早期对话里有大量的 message。这个时候你需要执行一个指令：`/compact`。

它可以将你的前面的 message 进行压缩。也就是说，你的 base 变小了，后面再对话的平方级别变化也就少了，就省下了 token。

## 给使用者的建议

### 推荐做法

我现在更推荐的方式其实是三步走，而不是像微信聊天一样一句一句往前推：

1. **先用一个会话，把要做什么整体沟通清楚**。任务目标是什么、范围在哪、有哪些约束、最后要交付什么，尽量一次讲明白。
2. **把共识落到文档里**。可以是 plan、todo、需求说明、任务拆解，总之要有一个能承接上下文的文字载体，别把关键信息只留在聊天历史里。
3. **然后 `/clear`，开一个新会话执行任务**。新会话只围绕这份文档和当前任务往下走，不要再背着前面那一大串沟通过程一起前进。

这样做的好处是：前一个会话负责"想清楚"，后一个会话负责"做干净"。既能减少上下文膨胀，也能让模型在执行阶段更聚焦，不容易像长聊天那样越聊越散、越聊越贵。

省着用上下文，才能让它一直保持又快又准——这跟收拾书桌是一个道理：东西堆得越多，找起来越慢，越容易把重要的东西压在底下忘掉。

### 不推荐做法

把多个完全不同的事情，在一个对话里解决。比如当你已经对话 20 轮后，没有及时 `/clear`，反而又开了一件毫不相干的事情。你会发现你的 token 消耗速度会特别快。血泪经验。
